Screenshots for the Appointment walkthrough.

Place the following files here (filenames below should match the references in the README):

- 01_login.png       — close-up of login input showing injected username (e.g., `admin' #`)
- 02_flag.png        — success page showing the retrieved flag
- 03_login_full.png  — full view of the login page
- 04_sqlmap.png      — sqlmap terminal output confirming injection

Add the PNG files with the exact filenames above so the README image links display correctly.
